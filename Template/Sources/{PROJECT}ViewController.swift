import UIKit
import Core

public final class {PROJECT}ViewController: BaseViewController<{PROJECT}State, {PROJECT}Action, {PROJECT}Context> {

    public override func setup(context: {PROJECT}Context) {
        super.setup(context: context)
        
        self.stateBindings = [
        ]
        
        self.actionBindings = [
        ]
    }
}
